{
	"AWSTemplateFormatVersion": "2010-09-09",
	"Description": "The parameters used by ${stack}-${instance}.",
	"Parameters": {
		"EncryptionKey": {
			"Description": "Depricated. Will be removed",
			"Type": "String",
			"NoEcho": true
		}
	},
	"Resources": {
		"IdGenertorUserName": {
			"Type": "AWS::SSM::Parameter",
			"Properties": {
				"Name": "${stack}.${instance}.org.sagebionetworks.id.generator.database.username",
				"Description": "Username for the ID generator database",
				"Type": "String",
				"Value": "${stack}idgenuser"
			}
		},
		"IdGenertorConnectionUrl": {
			"Type": "AWS::SSM::Parameter",
			"Properties": {
				"Name": "${stack}.${instance}.org.sagebionetworks.id.generator.database.connection.url",
				"Description": "The ID generator database connection URL",
				"Type": "String",
				"Value": "jdbc\\:mysql\\://${stack}-id-generator-db.${dbEndpointSuffix}/${stack}idgen"
			}
		},
		"ReopDatabaseUserName": {
			"Type": "AWS::SSM::Parameter",
			"Properties": {
				"Name": "${stack}.${instance}.org.sagebionetworks.repository.database.username",
				"Description": "Repo database username",
				"Type": "String",
				"Value": "${stack}${instance}user"
			}
		},
		"ReopDatabaseConnectionUrl": {
			"Type": "AWS::SSM::Parameter",
			"Properties": {
				"Name": "${stack}.${instance}.org.sagebionetworks.repository.database.connection.url",
				"Description": "Repo database connection URL",
				"Type": "String",
				"Value": "jdbc\\:mysql\\://${stack}-${instance}-db.${dbEndpointSuffix}/${stack}${instance}"
			}
		},
		"TablesDatabaseSchema": {
			"Type": "AWS::SSM::Parameter",
			"Properties": {
				"Name": "${stack}.${instance}.org.sagebionetworks.table.cluster.schema.0",
				"Description": "The schema of the table's database",
				"Type": "String",
				"Value": "${stack}${instance}user"
			}
		},
		"TablesDatabaseEndpoint": {
			"Type": "AWS::SSM::Parameter",
			"Properties": {
				"Name": "${stack}.${instance}.org.sagebionetworks.table.cluster.endpoint.0",
				"Description": "The endpoint of the table's database",
				"Type": "String",
				"Value": "${stack}-${instance}-table-0.${dbEndpointSuffix}"
			}
		},
		"TablesDatabaseCount": {
			"Type": "AWS::SSM::Parameter",
			"Properties": {
				"Name": "${stack}.${instance}.org.sagebionetworks.table.cluster.database.count",
				"Description": "The endpoint of the table's database",
				"Type": "String",
				"Value": "1"
			}
		},
		"DatabaseDriver": {
			"Type": "AWS::SSM::Parameter",
			"Properties": {
				"Name": "${stack}.${instance}.org.sagebionetworks.repository.databaes.driver",
				"Description": "The database driver used for all MySQL database connections",
				"Type": "String",
				"Value": "com.mysql.jdbc.Driver"
			}
		},
		"RepoDataS3Bucket": {
			"Type": "AWS::SSM::Parameter",
			"Properties": {
				"Name": "${stack}.${instance}.org.sagebionetworks.s3.bucket",
				"Description": "Main S3 bucket for all repository data",
				"Type": "String",
				"Value": "${stack}data.sagebase.org"
			}
		},
		"DoiUserName": {
			"Type": "AWS::SSM::Parameter",
			"Properties": {
				"Name": "${stack}.${instance}.org.sagebionetworks.ezid.username",
				"Description": "Username for the EZID doi service",
				"Type": "String",
				"Value": "sagebio"
			}
		},
		"DoiPrefix": {
			"Type": "AWS::SSM::Parameter",
			"Properties": {
				"Name": "${stack}.${instance}.org.sagebionetworks.ezid.doi.prefix",
				"Description": "The prefix of all DOIs",
				"Type": "String",
				"Value": "doi\\:10.7303/"
			}
		}
	}
}
